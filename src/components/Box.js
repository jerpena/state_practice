function Box(props) {
    const styles = {
        backgroundColor: "transparent"
    };

    return (
        <div
            style={styles}
            className="box"
        ></div>
    );
}

export default Box;